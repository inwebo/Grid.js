export const demoGrid = () => {
    const formCols   = document.getElementById('cols');
    const formRows   = document.getElementById('rows');
    const tileSize   = document.getElementById('tileSize');
    const tileOffset = document.getElementById('tileOffset');

    console.log(formCols, formRows, tileSize, tileOffset);

    formCols.onchange =(event)=> {

    };
};
